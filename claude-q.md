Where does the signed order get POSTed to Polymarket's CLOB from? v2 leans toward "bot posts directly from a non-blocked region" (Option A). Since the bot already runs on the Oracle VM — what region is that VM in? If it's a geoblocked region (e.g. a US Oracle region), the submit will fail and I need to spec a fix (move the VM, or a small relay in BR/SE/IE). Answer: region name, or "not sure."
Does the existing prepare/notify pipeline already build a full EIP-712 typed-data payload that a wallet can sign, or does it assume the browser's ClobClient does the signing internally? This decides whether the bot reuses prepare as-is and only adds the WalletConnect signature step, or whether I need to spec a new "build raw typed-data for external signer" path. Answer: "prepare returns typed-data" / "browser ClobClient signs" / "not sure — research the repo pattern."
For intent → trade, do you want a hard "echo + explicit confirm" step in chat before the wallet popup fires (v2 suggests echoing intent), or is the wallet popup itself the only confirmation? This affects the handler state machine I spec. Answer: "yes, confirm step" / "wallet popup is enough."